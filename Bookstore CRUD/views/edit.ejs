<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Update Book Store</title>

<link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;600;700&display=swap" rel="stylesheet">

<style>
body{
  margin:0;
  font-family:'Poppins',sans-serif;
  background:#f4f7ff;
}

.header{
  width:100%;
  padding:20px 0;
  background:linear-gradient(to right,#7aa8ff,#9fc5ff);
  text-align:center;
  color:white;
  border-bottom-left-radius:40px;
  border-bottom-right-radius:40px;
  box-shadow:0 10px 30px rgba(0,0,0,.18);
}

.header h1{
  font-size:28px;
  margin:0;
  letter-spacing:2px;
}

.main-area{
  height:calc(100vh - 110px);
  display:flex;
  align-items:center;
  justify-content:center;
  padding:0 20px;
}

.card{
  background:white;
  border-radius:26px;
  display:grid;
  grid-template-columns:1fr 1.2fr;
  gap:30px;
  padding:30px;
  width:100%;
  max-width:1000px;
  box-shadow: rgba(50,50,93,0.12) 0px 25px 45px,
              rgba(0,0,0,0.08) 0px 15px 25px;
}

.left{
  text-align:center;
  display:flex;
  flex-direction:column;
  justify-content:center;
  align-items:center;
}

.left h2{
  color:#4b4b4b;
  font-size:18px;
  margin-bottom:8px;
}

.book-img{
  width:100%;
  max-width:250px;
  height:300px;
  object-fit:cover;
  border-radius:18px;
  border:2px solid #e8edff;
  background:#f8faff;
  box-shadow:0 20px 35px rgba(0,0,0,0.2);
}

.right form{
  display:grid;
  gap:14px;
}

input[type="text"],
select{
  width:100%;
  height:50px;
  border-radius:14px;
  border:1px solid #d2d8ff;
  padding:0 14px;
  font-size:14px;
  background:#f8faff;
  outline:none;
  transition:.3s;
  box-sizing:border-box;
  display:block;
}

input[type="text"]:focus,
select:focus{
  border-color:#6f87ff;
  box-shadow:0 0 0 3px rgba(124,136,255,.28);
}

button{
  height:52px;
  border-radius:14px;
  border:none;
  background:linear-gradient(to right,#7ea5ff,#a9c6ff);
  color:white;
  font-size:17px;
  font-weight:700;
  cursor:pointer;
  transition:.3s;
}

button:hover{
  transform:translateY(-2px);
  box-shadow:0 18px 30px rgba(0,0,0,.23);
}

@media(max-width:900px){
  .card{
    grid-template-columns:1fr;
    text-align:center;
  }
}
</style>
</head>

<body>

<div class="header">
  <h1>📚 Update</h1>
</div>

<div class="main-area">
<div class="card">

  <div class="left">
      <h2>Book Preview</h2>
      <img class="book-img"
       src="<%=singleData.image ? singleData.image : 'https://via.placeholder.com/250x320.png?text=Book'%>">
  </div>

  <div class="right">
      <form action="/updateData" method="post">

          <input type="text" name="id" value="<%=singleData.id%>" hidden>

          <input type="text" name="name" value="<%=singleData.name%>" placeholder="Book Name">
          <input type="text" name="author" value="<%=singleData.author%>" placeholder="Author">
          <input type="text" name="price" value="<%=singleData.price%>" placeholder="Price">

          <select name="category">
            <option hidden>Choose Category</option>
            <option value="novel" <%=singleData.category == 'novel' ? 'selected' : ""%>>Novel</option>
            <option value="Study" <%=singleData.category == 'Study' ? 'selected' : ""%>>Study</option>
            <option value="comics" <%=singleData.category == 'comics' ? 'selected' : ""%>>Comics</option>
            <option value="motivation" <%=singleData.category == 'motivation' ? 'selected' : ""%>>Motivation</option>
          </select>

          <select name="lang">
            <option hidden>Choose Language</option>
            <option value="gujrati" <%=singleData.lang == 'gujrati' ? 'selected' : ""%>>Gujarati</option>
            <option value="hindi" <%=singleData.lang == 'hindi' ? 'selected' : ""%>>Hindi</option>
            <option value="english" <%=singleData.lang == 'english' ? 'selected' : ""%>>English</option>
          </select>

          <input type="text" name="image" value="<%=singleData.image%>" placeholder="Image URL">

          <button type="submit">Update Book</button>
      </form>
  </div>

</div>
</div>

</body>
</html>
