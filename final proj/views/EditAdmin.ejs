<!DOCTYPE html>
<html lang="en">

<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Edit Task</title>

<script src="https://cdn.tailwindcss.com"></script>
<link href="https://fonts.googleapis.com/css2?family=Quicksand:wght@400;600;700&display=swap" rel="stylesheet">

<style>
body{
  font-family:'Quicksand',sans-serif;
  background:#fdfaff;
  overflow:hidden;
  position:relative;
}

/* 🌸 Floating pastel blobs */
body::before{
  content:"";
  position:fixed;
  width:500px;
  height:500px;
  background:radial-gradient(circle,#ffd6e8,transparent 70%);
  top:-150px;
  left:-150px;
  animation:float1 12s infinite alternate ease-in-out;
  z-index:-1;
}

body::after{
  content:"";
  position:fixed;
  width:500px;
  height:500px;
  background:radial-gradient(circle,#d6e4ff,transparent 70%);
  bottom:-150px;
  right:-150px;
  animation:float2 15s infinite alternate ease-in-out;
  z-index:-1;
}

@keyframes float1{
  from{transform:translate(0,0)}
  to{transform:translate(60px,40px)}
}

@keyframes float2{
  from{transform:translate(0,0)}
  to{transform:translate(-60px,-40px)}
}

/* 💎 Glass Form Card */
.glass{
  background:rgba(255,255,255,0.7);
  backdrop-filter:blur(18px);
  border:1px solid rgba(255,255,255,0.6);
  box-shadow:0 20px 60px rgba(214,228,255,0.3);
  border-radius:28px;
  animation:fadeUp 0.8s ease;
}

/* ✨ Input styling */
input{
  border-radius:14px !important;
  transition:all 0.3s ease;
}

input:focus{
  transform:scale(1.03);
  box-shadow:0 10px 25px rgba(255,182,193,0.3);
  border-color:#ffb6c1 !important;
}

/* 🌟 Button */
.update-btn{
  background:linear-gradient(135deg,#ff9ecf,#a8c8ff);
  transition:all 0.4s ease;
  border-radius:14px;
}

.update-btn:hover{
  transform:scale(1.07);
  box-shadow:0 15px 40px rgba(214,228,255,0.5);
}

/* 🎀 Fade animation */
@keyframes fadeUp{
  from{opacity:0; transform:translateY(30px)}
  to{opacity:1; transform:translateY(0)}
}
</style>

</head>

<body class="flex items-center justify-center min-h-screen p-6">

<div class="glass p-10 w-full max-w-md">

<h1 class="text-3xl font-bold mb-8 text-center text-gray-700">
🌸 Edit Your Task
</h1>

<form action="/task/updateData" method="post" class="space-y-5">

<input type="hidden" name="id" value="<%=singleData.id%>">

<input type="text" name="task" value="<%=singleData.task%>"
placeholder="Enter Your Task"
class="w-full px-4 py-3 border border-gray-200 focus:outline-none focus:ring-2 focus:ring-pink-200" required>

<input type="text" name="description" value="<%=singleData.description%>"
placeholder="Enter Your Description"
class="w-full px-4 py-3 border border-gray-200 focus:outline-none focus:ring-2 focus:ring-pink-200" required>

<input type="text" name="status" value="<%=singleData.status%>"
placeholder="Enter Your Status"
class="w-full px-4 py-3 border border-gray-200 focus:outline-none focus:ring-2 focus:ring-pink-200" required>

<input type="text" name="priority" value="<%=singleData.priority%>"
placeholder="Enter Your Priority"
class="w-full px-4 py-3 border border-gray-200 focus:outline-none focus:ring-2 focus:ring-pink-200" required>

<input type="date" name="date" value="<%=singleData.date%>"
class="w-full px-4 py-3 border border-gray-200 focus:outline-none focus:ring-2 focus:ring-pink-200" required>

<button type="submit"
class="update-btn w-full text-white py-3 font-semibold">
Update Task ✨
</button>

</form>

</div>

</body>

</html>