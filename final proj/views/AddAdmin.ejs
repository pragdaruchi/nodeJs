<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Add Task</title>

<script src="https://cdn.tailwindcss.com"></script>
<link href="https://fonts.googleapis.com/css2?family=Quicksand:wght@400;600;700&display=swap" rel="stylesheet">

<style>
body{
  font-family:'Quicksand',sans-serif;
  background:#fdfaff;
  overflow:hidden;
  position:relative;
}

body::before{
  content:"";
  position:absolute;
  width:500px;
  height:500px;
  background:radial-gradient(circle,#ffd6e8,transparent 70%);
  top:-150px;
  left:-150px;
  animation:float1 12s infinite alternate ease-in-out;
  z-index:-1;
}

body::after{
  content:"";
  position:absolute;
  width:500px;
  height:500px;
  background:radial-gradient(circle,#d6e4ff,transparent 70%);
  bottom:-150px;
  right:-150px;
  animation:float2 15s infinite alternate ease-in-out;
  z-index:-1;
}

@keyframes float1{
  from{transform:translate(0,0)}
  to{transform:translate(60px,40px)}
}

@keyframes float2{
  from{transform:translate(0,0)}
  to{transform:translate(-60px,-40px)}
}

/* 💎 Glass Card */
.glass{
  background:rgba(255,255,255,0.65);
  backdrop-filter:blur(20px);
  border:1px solid rgba(255,255,255,0.6);
  box-shadow:0 20px 60px rgba(255,182,193,0.2);
  transition:all 0.4s ease;
}

.glass:hover{
  transform:translateY(-8px);
  box-shadow:0 30px 80px rgba(214,228,255,0.4);
}

input{
  transition:all 0.3s ease;
  border-radius:12px;
}

input:focus{
  border-color:#a78bfa;
  box-shadow:0 0 0 4px rgba(167,139,250,0.2);
  transform:scale(1.03);
}

button{
  background:linear-gradient(135deg,#ffd6e8,#d6e4ff);
  font-weight:600;
  border-radius:14px;
  transition:all 0.3s ease;
}

button:hover{
  transform:scale(1.08);
  box-shadow:0 15px 40px rgba(255,182,193,0.5);
  background:linear-gradient(135deg,#ffb3d9,#bcd4ff);
}

.fadeUp{
  animation:fadeUp 0.8s ease forwards;
}

@keyframes fadeUp{
  from{opacity:0; transform:translateY(40px)}
  to{opacity:1; transform:translateY(0)}
}
</style>

</head>

<body class="flex items-center justify-center min-h-screen">

<div class="glass p-10 rounded-3xl w-full max-w-lg fadeUp">

    <h1 class="text-3xl font-bold mb-8 text-center text-gray-700">
        ✨ Add New Task
    </h1>

    <form action="/task/AddAdmin" method="post" class="space-y-5">

        <input type="text" name="task" placeholder="Enter Your Task"
        class="w-full px-4 py-3 border border-gray-200 focus:outline-none" required>

        <input type="text" name="description" placeholder="Enter Your Description"
        class="w-full px-4 py-3 border border-gray-200 focus:outline-none" required>

        <input type="text" name="status" placeholder="Enter Your Status"
        class="w-full px-4 py-3 border border-gray-200 focus:outline-none" required>

        <input type="text" name="priority" placeholder="Enter Your Priority"
        class="w-full px-4 py-3 border border-gray-200 focus:outline-none" required>

        <input type="date" name="date"
        class="w-full px-4 py-3 border border-gray-200 focus:outline-none" required>

        <button type="submit"
        class="w-full py-3 text-gray-700">
        Submit Task 
        </button>

    </form>

</div>

</body>
</html>