<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Sky Blue To-Do List</title>
</head>

<body style="
  font-family:'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
  margin:0;
  padding:30px;
  min-height:95vh;
  display:flex;
  flex-direction:column;
  align-items:center;
  background:linear-gradient(135deg,#a3b5d7,#90b2e2,#80cfff,#2568b0);
">

  <h1 style="font-size:2.6rem;font-weight:900;color:#0b2545;margin-bottom:36px;">
    ☁️ To-Do List
  </h1>

  <form action="/addData" method="post" style="
      width:100%;max-width:520px;
      background:rgba(255,255,255,.92);
      padding:32px;border-radius:28px;
      border:1px solid rgba(255,255,255,.6);
      backdrop-filter:blur(12px);
      box-shadow:0 20px 40px rgba(0,0,0,.14);
      margin-bottom:42px;
  ">
    <h2 style="margin-bottom:18px;color:#0b2545;font-weight:700;">
      Add New Task
    </h2>

    <input type="text" name="name" placeholder="Enter your task..." required style="
        width:92%;padding:16px;border-radius:16px;
        border:1px solid #bcd7ff;background:#ffffff;
        outline:none;font-size:1rem;margin-bottom:20px;
    ">

    <button type="submit" style="
        width:100%;padding:14px 0;border-radius:18px;
        border:none;cursor:pointer;color:white;
        font-weight:700;font-size:1.05rem;
        background:linear-gradient(to right,#6fb6ff,#4ba3ff,#2f7fff);
    ">
      Add Task
    </button>
  </form>

  <div style="width:100%;max-width:520px;display:grid;gap:18px;">
    <% data.map((e)=>{ %>
      <div style="
        background:rgba(255,255,255,.95);
        border-radius:24px;
        padding:22px;
        border:1px solid rgba(255,255,255,.7);
        box-shadow:0 18px 32px rgba(0,0,0,.12);
        display:flex;justify-content:space-between;align-items:center;
      ">
        <p style="margin:0;font-size:1.15rem;font-weight:700;color:#0b2545;">
          <%= e.name %>
        </p>

        <div style="display:flex;gap:10px;">
          <a href="/editData?id=<%= e.id %>" style="
              text-decoration:none;color:#083b74;font-weight:600;
              padding:8px 16px;border-radius:16px;
              background:linear-gradient(to right,#d8ecff,#a9d6ff);
            ">
            Edit
          </a>
          <a href="/deleteData/<%= e.id %>"style="text-decoration:none;color:white;font-weight:600;padding:8px 16px;border-radius:16px;background:#ffa7a7;">Delete</a>
        </div>
      </div>
      <% }) %>
  </div>

</body>

</html>