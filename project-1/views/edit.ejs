<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Update To-Do</title>
</head>
<body style="font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif; min-height:100vh; margin:0; padding:40px; display:flex; flex-direction:column; align-items:center; background: linear-gradient(to right, #fbc2eb, #a6c1ee);">
  <h1 style="font-size:2.5rem; font-weight:800; margin-bottom:40px; color:#1f2937; text-shadow:1px 1px 3px rgba(0,0,0,0.2);">Update To-Do List</h1>
  <form action="/updateData" method="post" 
        style="width:100%; max-width:500px; background: rgba(255,255,255,0.85); backdrop-filter: blur(12px); padding:36px; border-radius:28px; box-shadow:0 18px 36px rgba(0,0,0,0.15); border:1px solid rgba(255,255,255,0.25); box-sizing:border-box;">
    <input type="text" name="id" value="<%= singleData.id %>" hidden>
    <input type="text" name="name" placeholder="Enter Name" value="<%= singleData.name %>" required style="width:100%; padding:18px; margin-bottom:18px; border-radius:18px; border:1px solid #d1d5db; outline:none; font-size:1rem; box-sizing:border-box;">
    <input type="text" name="age" placeholder="Enter Age" value="<%= singleData.age %>" required style="width:100%; padding:18px; margin-bottom:18px; border-radius:18px; border:1px solid #d1d5db; outline:none; font-size:1rem; box-sizing:border-box;">
    <input type="text" name="city" placeholder="Enter City" value="<%= singleData.city %>" required style="width:100%; padding:18px; margin-bottom:24px; border-radius:18px; border:1px solid #d1d5db; outline:none; font-size:1rem; box-sizing:border-box;">
    <button type="submit" style="width:100%; padding:14px 0; border-radius:20px; border:none; font-weight:700; font-size:1.1rem; background: linear-gradient(to right, #f59e0b, #d97706); color:white; cursor:pointer;">Update To-Do</button>
  </form>
</body>
</html>
