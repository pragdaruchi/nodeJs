<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Update To-Do</title>
</head>

<body style="
  font-family:'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
  margin:0;
  padding:40px;
  min-height:90vh;
  display:flex;
  flex-direction:column;
  align-items:center;
  background:linear-gradient(135deg,#94addc,#a4bdde,#a5cbe5,#aac4e0);
">

  <h1 style="font-size:2.5rem;font-weight:900;color:#0b2545;margin-bottom:34px;">
    ✨ Update Task
  </h1>

  <form action="/updateData" method="post"
    style="
      width:100%;max-width:520px;
      background:rgba(255,255,255,.96);
      padding:34px;border-radius:28px;
      border:1px solid rgba(255,255,255,.7);
      box-shadow:0 20px 40px rgba(0,0,0,.15);
      backdrop-filter:blur(10px);
  ">

    <input type="text" name="id" value="<%= singleData.id %>" hidden>

    <input type="text" name="name" value="<%= singleData.name %>"
      placeholder="Enter Task"
      style="
        width:90%;padding:18px;border-radius:18px;
        border:1px solid #bcd7ff;background:#ffffff;
        font-size:1rem;margin-bottom:22px;outline:none;
    ">

    <button type="submit"
      style="
        width:100%;padding:14px 0;border-radius:18px;border:none;
        font-weight:700;font-size:1.1rem;color:white;cursor:pointer;
        background:linear-gradient(to right,#6fb6ff,#4ba3ff,#2f7fff);
    ">
      Update Task
    </button>

  </form>

</body>
</html>
